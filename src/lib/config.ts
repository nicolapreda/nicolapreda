import { dev } from '$app/environment';

// place files you want to import through the `$lib` alias in this folder.
export const title = "Nicola Preda"
export const description = "Nicola Preda's personal website"
export const url = dev ? "http://localhost:3000" : "https://predanicola.it"