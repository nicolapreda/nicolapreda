<%- include('partials/header') %>

    <main
        class="pt-32 pb-20 px-6 sm:px-12 bg-primary text-white min-h-screen flex flex-col items-center justify-center">
        <div class="max-w-2xl mx-auto text-center">
            <!-- Construction Icon / Visual -->
            <div class="mb-8 animate-pulse text-6xl">
                🚧
            </div>

            <h1 class="text-6xl sm:text-8xl font-black mb-6 tracking-tighter uppercase">
                Work in <br class="hidden sm:block" />
                <span class="font-serif italic">Progress</span>
            </h1>

            <p class="text-xl sm:text-2xl text-white/80 mb-12 font-sans font-medium leading-relaxed">
                Sto lavorando a questa sezione per portarti i migliori progetti Web & Lead Gen.
                Torna presto per vedere il risultato!
            </p>

            <a href="/"
                class="inline-block border-2 border-white px-8 py-3 text-sm font-bold uppercase tracking-widest hover:bg-white hover:text-primary transition-all">
                Torna alla Home
            </a>
        </div>
    </main>

    <%- include('partials/footer') %>